# Codex host notes

- Prefer invoking the Agent-neutral `cq` binary rather than re-implementing rules.
- Keep global Codex `AGENTS.md` as a router to project `AGENTS.md`.
- Do not embed provider credentials, forge tokens, or raw model transcripts in
  repository files.
