## Code Quality CLI

Use the Agent-neutral `cq` CLI for deterministic validation and review
orchestration. Shared rules remain in this `AGENTS.md`. Do not copy machine
rule JSON/YAML into Skills or prompts.
